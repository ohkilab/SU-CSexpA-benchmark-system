syntax = "proto3";

import "benchmark/messages.proto";

option go_package = "./benchmark";

service BenchmarkService {
  rpc CreateTask(CreateTaskRequest) returns (CreateTaskResponse);
  rpc GetTaskProgress(GetTaskProgressRequest)
      returns (stream GetTaskProgressResponse);
}
